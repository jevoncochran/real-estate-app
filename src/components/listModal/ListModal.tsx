import React from "react";

interface ListModalProps {
  handleHideListModal: () => void;
}

const ListModal = ({ handleHideListModal }: ListModalProps) => {
  return <div>ListModal</div>;
};

export default ListModal;
